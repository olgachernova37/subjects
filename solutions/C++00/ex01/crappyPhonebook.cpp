#include <iostream>
#include <cctype>
#include "Phonebook.hpp"
using namespace std;

int main()
{

    while(true)
    {
        cout << "Write a command: ";
        string command;
        cin >> command;
        if (command == "ADD")
        {

        }
        else if (command == "SEARCH")
        {

        }
        else if (command == "EXIT")
        {
            break;
        }
    }
    return (0);
}
